import { FilterPsicologosPipe } from './filter-psicologos.pipe';

describe('FilterPsicologosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPsicologosPipe();
    expect(pipe).toBeTruthy();
  });
});
